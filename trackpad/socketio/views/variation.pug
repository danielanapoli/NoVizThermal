html(lang="en")

    head
        meta(charset="utf-8")
        title Usability Testing Administration Page

        //- JS
        script(src="/socket.io/socket.io.js")
        script(src="../communication.js")

        include ../partials/bootstrap.pug

    body
        include ../partials/header.pug
            //- The next is indented so it is considered part of the div in hearder.pug
            hr(class="my-4")
            a(class="btn btn-secondary btn-sm" href="/" role="button") Start over

        div(class="container")
            div(class="alert alert-warning" role="alert")
                h3 Before you start! 
                p To maintain the integrity of our data, you cannot visit a test website more than once. Be sure of your assessment before closing a tab. You will not be able to reopen a test site.

        div(class="container")
            form(method="post", action="/handler")
                div(class="form-group")
                    input(class="form-control" type="text" name="participantID" placeholder="Participant ID" required)
                    input(class="form-control" type="text" name="variation" value="" + id hidden)
                br

                each key in Object.keys(variation)
                    //- WEBSITE
                    //- a(href="" + variation[key][0] id="" + variation[key][1] target="_blank" onclick="getElementById('open_site1').value=Date();linkDisable(this);heaterON()"): h3 Website #{key}
                    a(href="" + variation[key][0] id="" + variation[key][1] target="_blank" onclick="openSiteHandler(this, " + variation[key][1] + ", " + key + ")"): h3 Website #{key}
                    input(class="form-control" type="text" name="order" + key id="order" + key value = "" + key hidden)
                    input(class="form-control" type="text" name="open_site" + key id="open_site" + key hidden)
                    br

                    //- Safe/Unsafe
                    label(class="assessment") The above website is...
                        input(class="form-control" type="text" name="close_site" + key id="close_site" + key hidden)

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="assessment_site" + key id="assessment1" + key value="high" onclick="closeSiteHandler(" + key + ")" required disabled)
                            label(class="form-check-label" for="assessment1" + key) High risk

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="assessment_site" + key id="assessment2" + key value="low" onclick="closeSiteHandler(" + key + ")" required disabled)
                            label(class="form-check-label" for="assessment2" + key) Low risk
                    br

                    //- Confidence Rating
                    label(class="confidence-level") I am confident in my assessment.

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="confidence_site" + key id= "confidence3" + key value="0" required disabled)
                            label(class="form-check-label" for="confidence3" + key) 0 - Strongly Disagree

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="confidence_site" + key id="confidence4" + key value="1" required disabled)
                            label(class="form-check-label" for="confidence4" + key) 1 - Disagree

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="confidence_site" + key id="confidence5" + key value="2" required disabled)
                            label(class="form-check-label" for="confidence5" + key) 2 - Neutral

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="confidence_site" + key id="confidence6" + key value="3" required disabled)
                            label(class="form-check-label" for="confidence6" + key) 3 - Agree

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="confidence_site" + key id="confidence7" + key value="4" required disabled)
                            label(class="form-check-label" for="confidence7" + key) 4 - Strongly Agree
                    br

                    //- Ease Rating
                    label(class="confidence-level") It was easy for me to assess this website.

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="ease_site" + key id="ease8" + key value="0" required disabled)
                            label(class="form-check-label" for="ease8" + key) 0 - Strongly Disagree

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="ease_site" + key id="ease9" + key value="1" required disabled)
                            label(class="form-check-label" for="ease9" + key) 1 - Disagree

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="ease_site" + key id="ease10" + key value="2" required disabled)
                            label(class="form-check-label" for="ease10" + key) 2 - Neutral

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="ease_site" + key id="ease11" + key value="3" required disabled)
                            label(class="form-check-label" for="ease11" + key) 3 - Agree

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="ease_site" + key id="ease12" + key value="4" required disabled)
                            label(class="form-check-label" for="ease12" + key) 4 - Strongly Agree      
                    br

                    hr

                button(type="submit" class="btn btn-primary") Submit Assessments
                br