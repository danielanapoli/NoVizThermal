html(lang="en")

    head
        meta(charset="utf-8")
        title Usability Testing Administration Page

        //- JS
        script(src="/socket.io/socket.io.js")
        script(src="../communication.js")

        include ../partials/bootstrap.pug

    body
        include ../partials/header.pug
            //- The next is indented so it is considered part of the div in hearder.pug
            hr(class="my-4")
            a(class="btn btn-secondary btn-sm" href="/" role="button") Start over

        div(class="container")
            div(class="alert alert-warning" role="alert")
                h3 Before you start! 
                p To maintain the integrity of our data, you cannot visit a test website more than once. Be sure of your assessment before closing a tab. You will not be able to reopen a test site.

        div(class="container")
            form(method="post", action="/handler")
                //- div(class="form-group")
                    input(class="form-control" type="text" name="participantID" placeholder="Participant ID" required)
                    //- input(class="form-control" type="text" name="variation" value="" + id hidden)
                //- br

                each website, index in variation
                    //- WEBSITE
                    a(href="" + website[0] id="" + website[1] target="_blank" onclick="openSiteHandler(this, " + website[1] + ", " + (index + 1) + ")"): h3 Website #{(index + 1)}
                    input(class="form-control" type="text" name="order" + (index + 1) id="order" + (index + 1) value = "" + (index + 1) hidden)
                    input(class="form-control" type="text" name="open_site" + (index + 1) id="open_site" + (index + 1) hidden)
                    br

                    //- Safe/Unsafe
                    label(class="assessment") The above website is...
                        input(class="form-control" type="text" name="close_site" + (index + 1) id="close_site" + (index + 1) hidden)

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="assessment_site" + (index + 1) id="assessment1" + (index + 1) value="high" onclick="closeSiteHandler(" + (index + 1) + ")" required disabled)
                            label(class="form-check-label" for="assessment1" + (index + 1)) High risk

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="assessment_site" + (index + 1) id="assessment2" + (index + 1) value="low" onclick="closeSiteHandler(" + (index + 1) + ")" required disabled)
                            label(class="form-check-label" for="assessment2" + (index + 1)) Low risk
                    br

                    //- Confidence Rating
                    label(class="confidence-level") I am confident in my assessment.

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="confidence_site" + (index + 1) id= "confidence3" + (index + 1) value="0" required disabled)
                            label(class="form-check-label" for="confidence3" + (index + 1)) 0 - Strongly Disagree

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="confidence_site" + (index + 1) id="confidence4" + (index + 1) value="1" required disabled)
                            label(class="form-check-label" for="confidence4" + (index + 1)) 1 - Disagree

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="confidence_site" + (index + 1) id="confidence5" + (index + 1) value="2" required disabled)
                            label(class="form-check-label" for="confidence5" + (index + 1)) 2 - Neutral

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="confidence_site" + (index + 1) id="confidence6" + (index + 1) value="3" required disabled)
                            label(class="form-check-label" for="confidence6" + (index + 1)) 3 - Agree

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="confidence_site" + (index + 1) id="confidence7" + (index + 1) value="4" required disabled)
                            label(class="form-check-label" for="confidence7" + (index + 1)) 4 - Strongly Agree
                    br

                    //- Ease Rating
                    label(class="confidence-level") It was easy for me to assess this website.

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="ease_site" + (index + 1) id="ease8" + (index + 1) value="0" required disabled)
                            label(class="form-check-label" for="ease8" + (index + 1)) 0 - Strongly Disagree

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="ease_site" + (index + 1) id="ease9" + (index + 1) value="1" required disabled)
                            label(class="form-check-label" for="ease9" + (index + 1)) 1 - Disagree

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="ease_site" + (index + 1) id="ease10" + (index + 1) value="2" required disabled)
                            label(class="form-check-label" for="ease10" + (index + 1)) 2 - Neutral

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="ease_site" + (index + 1) id="ease11" + (index + 1) value="3" required disabled)
                            label(class="form-check-label" for="ease11" + (index + 1)) 3 - Agree

                        div(class="form-check")
                            input(class="form-check-input" type="radio" name="ease_site" + (index + 1) id="ease12" + (index + 1) value="4" required disabled)
                            label(class="form-check-label" for="ease12" + (index + 1)) 4 - Strongly Agree      
                    br

                    hr

                button(type="submit" class="btn btn-primary") Submit Assessments
                br